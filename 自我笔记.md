# 7/21

## TIM3 独立验证
TIM3正确计数

## 尝试读回复TMC2209
 自环回通过，在关中断中进行收发，无回复。 **TODO**:发送指令操作电机运作，若电机不动，则说明TMC2209没收到数据帧

# 7/22
## TMC2209测试
读写都成功，过滤毛刺再读取不然没有读回复，芯片的8号5vout引脚电压不稳定，5v时才能正常读写，待芯片修复(7/23已修复，稳定5v);TMC2209要读写要放在程序最开始，不然很难抓到信号与之通信。地址是00


## TODO:烧录时给U4生成唯一的SN码
### 思路
用U4的UID做处理当SN码。每块U4编程时读自己的UID再写回去
### 过程构思
U4版放到治具上->U1 ISP读U4的UID地址（固定）->拿到UID->U1 ISP写UID 到 FLASH末尾待定区域（不占用固件空间）->烧固件

# 7/23
## 电机测试
###   遗留问题：HAL_TIM_PWM_Start_IT 不设 UIE

  需要手动补 TIM3->DIER |= TIM_DIER_UIE 才能工作。可能是 TIM3 被
  tim3_save/tim3_restore 接管后 HAL 内部状态与硬件不一致。后续可以在
  tim3_restore 里加上 htim3.Instance->DIER 同步修复。

## 测电机相关的引脚电压
  TMC22引脚vs->24V;

  (u7)PA5上电时变成0v，稳定后为3.3v;
  
  (U7)PB4(MCPWM_DIR2):0V 和3.3V PB5(MCPWM_STEP2):交流档测上电时电压一直在变化，最高到达1.9V
### 硬件有问题，已修复
修复前功率仅0.4w，电流进不去电机，光响不转。修复后电机可转动，功率可达5W，顺逆时针正常转动

## 电机总结
  电机正常转动，TMC2209温度在电机运作时50℃。

### 待优化：

    HAL_TIM_PWM_Start_IT 漏设 UIE, 需手动 TIM3->DIER |= TIM_DIER_UIE;->  STEP 每发一个脉冲，电机走一步。TIM3 负责数步数——走到目标步数就停机。数步靠的是"更新中断"——脉冲结束时 CPU 被唤醒去 +1。HAL_TIM_PWM_Start_IT 本应自动开这个中断，但它没开。因为中间我们接管过 TIM3（给 TMC2209 做 UART），还回去后 HAL状态乱了。表现就是：电机不会自己停，超时报错。加 TIM3->DIER |= TIM_DIER_UIE 手动把中断开了就好。

## USB CDC修复
插上识别不出端口。7/25修复，F103系列不自带上拉电阻配给D+，添加后正常识别。

# 7/25
# PC下载固件到 SPI FLASH 设计思路
## 整体架构
**app.c**帧接收->APP_dispatch()命令路由->FW_START/DATA/END三段式上传处理->UploadData()缓冲-攒页-写FLash **|** ->**gd25q128.c**->擦除->写入
## PC传固件设置为三段式
固件大，一帧无法传完需拆成多帧。因此需要知道什么时候开始START(0X0009),数据DATA*N(0X000A),结束END(0X000B)。无START则不知写到哪里，有多大。无END则不知是否传完，残页没法刷，字节数无法比对。

## 状态机设计
**app_state:**
IDLE->[START]->UPLOADING->[DATA....DATA]->END->IDLE
　　　　　　　　　　|__10s超时->IDLE
　　　　　　　　　　|__溢出->锁存错误(继续收ACK)
　　　　　　　　　　|__Flash写入失败->IDLE
## 双槽设计
  烧录：
slot0:Bootloader 0x00000 64kb ->isp write->u4 0x080000000
slot1:APP 0x10000 64kb ->isp write->u4 0x080000000 + slot0_size
在FW_END后切换0/1，当两槽都有完整固件(判断长度及完整标志位flag 0xA5)时才能进行烧录，否则返回错误码。

## 固件下载边缘检测
  ┌──────────┬──────────────┬───────────────────────────┬──────┐
  │ 测试编号  │  边缘检测点   │         代码位置           │ 实现 │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [1]      │ 正常 START    │ app.c:489 写头+切状态      │ ✅  │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [2]      │ START BUSY   │ app.c:442 state!=IDLE →   │ ✅  │
  │          │              │ 0x01                      │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [3]      │ START LEN=6  │ app.c:445 len<9 → 0x02    │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [4]      │ START size=0 │ app.c:451 bytes==0 → 0x02 │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [5]      │ START        │ app.c:451 bytes>65528 →   │ ✅   │
  │          │ size超限      │ 0x02                     │       │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [6]      │ START        │ app.c:451 frames==0 →     │ ✅   │
  │          │ frames=0     │ 0x02                      │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [7]      │ 正常 DATA     │ app.c:506 UploadData()   │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [8]      │ DATA BUSY    │ app.c:497                 │ ✅   │
  │          │              │ state!=UPLOADING → 0x01   │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [9]      │ DATA         │ app.c:217 count+len>total │ ✅   │
  │          │ OVERFLOW     │  → 0x0D                   │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [10]     │ DATA TIMEOUT │ app.c:750 >10s → 0x0C     │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [11]     │ 错误锁存      │ app.c:500-502             │ ✅   │
  │          │              │ upload_error→直接回       │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [12]     │ 残页写入      │ app.c:530-532 page_idx>0  │ ✅   │
  │          │              │ → 刷入                    │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [13]     │ 正常 END     │ app.c:519-554 完整流程    │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [14]     │ END BUSY     │ app.c:520                 │ ✅   │
  │          │              │ state!=UPLOADING → 0x01   │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [15]     │ END          │ app.c:538 count≠total →   │ ✅   │
  │          │ ERR_COUNT    │ 0x0A                      │      │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [16]     │ 正常 QUERY   │ app.c:563-587 读头+回版本 │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [17]     │ QUERY NO_FW  │ app.c:575 size==0 → 0x06  │ ✅   │
  ├──────────┼──────────────┼───────────────────────────┼──────┤
  │ [18]     │ QUERY        │ app.c:578 flag≠0xA5 →     │ ✅   │
  │          │ INCOMPLETE   │ 0x07                      │      │
  └──────────┴──────────────┴───────────────────────────┴──────┘



 ## PC下载固件到U1报错边缘检测
 进行了18项测试用例，包括busy状态、overflow、timeout、error lock(某帧DATA出错，设置错误码永久锁住，后续DATA帧全返回锁存错误，防止后续DATA状态正常导致状态码被覆盖，新一轮上传或复位U1时恢复锁存状态)、error count、无固件、文件大小异常。

 ## u1->spi flash
 第一轮：FW_START->DATA->END->写入Slot0(bootloader,0x00000)->ACK 0X00->upload_slot 自动切到1
 
 第二轮：FW_START->DATA->END->写入Slot1(app,0x10000)->ACK 0X00->upload_slot 自动切到0

即发两轮指令，第一轮写入Slot0，第二轮写入Slot1，自动切换到0。

# 7/27
