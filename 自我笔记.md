# 7/21

## TIM3 独立验证

TIM3正确计数

## 尝试读回复TMC2209

自环回通过，在关中断中进行收发，无回复。 **TODO**:发送指令操作电机运作，若电机不动，则说明TMC2209没收到数据帧

# 7/22

## TMC2209测试

读写都成功，过滤毛刺再读取不然没有读回复，芯片的8号5vout引脚电压不稳定，5v时才能正常读写，待芯片修复(7/23已修复，稳定5v);TMC2209要读写要放在程序最开始，不然很难抓到信号与之通信。地址是00

## TODO:烧录时给U4生成唯一的SN码

### 思路

用U4的UID做处理当SN码。每块U4编程时读自己的UID再写回去

### 过程构思

U4版放到治具上->U1 ISP读U4的UID地址（固定）->拿到UID->U1 ISP写UID 到 FLASH末尾待定区域（不占用固件空间）->烧固件

# 7/23

## 电机测试

### 遗留问题：HAL\_TIM\_PWM\_Start\_IT 不设 UIE

需要手动补 TIM3->DIER |= TIM\_DIER\_UIE 才能工作。可能是 TIM3 被
tim3\_save/tim3\_restore 接管后 HAL 内部状态与硬件不一致。后续可以在
tim3\_restore 里加上 htim3.Instance->DIER 同步修复。

## 测电机相关的引脚电压

TMC2209的22引脚vs->24V;

(u7)PA5上电时变成0v，稳定后为3.3v;

(U7)PB4(MCPWM\_DIR2):0V 和3.3V PB5(MCPWM\_STEP2):交流档测上电时电压一直在变化，最高到达1.9V

### 硬件有问题，已修复

修复前功率仅0.4w，电流进不去电机，光响不转。修复后电机可转动，功率可达5W，顺逆时针正常转动

## 电机总结

电机正常转动，TMC2209温度在电机运作时50℃。

### 待优化：

```
HAL_TIM_PWM_Start_IT 漏设 UIE, 需手动 TIM3->DIER |= TIM_DIER_UIE;->  STEP 每发一个脉冲，电机走一步。TIM3 负责数步数——走到目标步数就停机。数步靠的是"更新中断"——脉冲结束时 CPU 被唤醒去 +1。HAL_TIM_PWM_Start_IT 本应自动开这个中断，但它没开。因为中间我们接管过 TIM3（给 TMC2209 做 UART），还回去后 HAL状态乱了。表现就是：电机不会自己停，超时报错。加 TIM3->DIER |= TIM_DIER_UIE 手动把中断开了就好。
```

## USB CDC修复

插上识别不出端口。7/25修复，F103系列不自带上拉电阻配给D+，添加后正常识别。

# 7/25

# PC下载固件到 SPI FLASH 设计思路

## 整体架构

**app.c**帧接收->APP\_dispatch()命令路由->FW\_START/DATA/END三段式上传处理->UploadData()缓冲-攒页-写FLash **|** ->**gd25q128.c**->擦除->写入

## PC传固件设置为三段式

固件大，一帧无法传完需拆成多帧。因此需要知道什么时候开始START(0X0009),数据DATA\*N(0X000A),结束END(0X000B)。无START则不知写到哪里，有多大。无END则不知是否传完，残页没法刷，字节数无法比对。

## 状态机设计

**app\_state:**

```text
IDLE->[START]->UPLOADING->[DATA....DATA]->END->IDLE
　　　　　　　　　　|__10s超时->IDLE
　　　　　　　　　　|__溢出->锁存错误(继续收ACK)
　　　　　　　　　　|__Flash写入失败->IDLE
```

## 双槽设计

烧录：
slot0:Bootloader 0x00000 64kb ->isp write->u4 0x08000000
slot1:APP 0x10000 64kb ->isp write->u4 0x08000000 + slot0\_size
在FW\_END后切换0/1，当两槽都有完整固件(判断长度及完整标志位flag 0xA5)时才能进行烧录，否则返回错误码。

## 固件下载边缘检测

| 测试编号  | 边缘检测点            | 代码位置                             |  实现 |
| :---- | :--------------- | :------------------------------- | :-: |
| \[1]  | 正常 START         | app.c:489 写头+切状态                 |  ✅  |
| \[2]  | START BUSY       | app.c:442 state!=IDLE →0x01      |  ✅  |
| \[3]  | START LEN=6      | app.c:445 len<9 → 0x02           |  ✅  |
| \[4]  | START size=0     | app.c:451 bytes==0 → 0x02        |  ✅  |
| \[5]  | START size超限     | app.c:451 bytes>65528 →0x02      |  ✅  |
| \[6]  | START frames=0   | app.c:451 frames==0 →0x02        |  ✅  |
| \[7]  | 正常 DATA          | app.c:506 UploadData()           |  ✅  |
| \[8]  | DATA BUSY        | app.c:497 state!=UPLOADING →0x01 |  ✅  |
| \[9]  | DATA OVERFLOW    | app.c:217 count+len>total →0x0D  |  ✅  |
| \[10] | DATA TIMEOUT     | app.c:750 >10s → 0x0C            |  ✅  |
| \[11] | 错误锁存             | app.c:500-502 upload\_error→直接回  |  ✅  |
| \[12] | 残页写入             | app.c:530-532 page\_idx>0 →刷入    |  ✅  |
| \[13] | 正常 END           | app.c:519-554 完整流程               |  ✅  |
| \[14] | END BUSY         | app.c:520 state!=UPLOADING →0x01 |  ✅  |
| \[15] | END ERR\_COUNT   | app.c:538 count≠total →0x0A      |  ✅  |
| \[16] | 正常 QUERY         | app.c:563-587 读头+回版本             |  ✅  |
| \[17] | QUERY NO\_FW     | app.c:575 size==0 → 0x06         |  ✅  |
| \[18] | QUERY INCOMPLETE | app.c:578 flag≠0xA5 →0x07        |  ✅  |

## PC下载固件到U1报错边缘检测

进行了18项测试用例，包括busy状态、overflow、timeout、error lock(某帧DATA出错，设置错误码永久锁住，后续DATA帧全返回锁存错误，防止后续DATA状态正常导致状态码被覆盖，新一轮上传或复位U1时恢复锁存状态)、error count、无固件、文件大小异常。

## u1->spi flash

第一轮：FW\_START->DATA->END->写入Slot0(bootloader,0x00000)->ACK 0X00->upload\_slot 自动切到1

第二轮：FW\_START->DATA->END->写入Slot1(app,0x10000)->ACK 0X00->upload\_slot 自动切到0

即发两轮指令，第一轮写入Slot0，第二轮写入Slot1，自动切换到0。

# 7/27

## 上位机软件+U1测试

新增转发查询U4版本号协议(PC->U1,U1->U4)，上位机协议向我对齐

# 7/28

## U1U7U4,4路ADC检测

添加协议，PC->U1可转发协议，查询U1U7U4的ADC值

## 测U4ADC值问题修复

U1与U7通讯不顺畅，U7的RX接收不到U1发送的指令。软件层面没问题，操作时忘记用跳帽将U1U7的RXTX短接。

# 7/29

## 读取flash固件烧录到测径板

**注意： 本ISP协议基于AN2606协议编写**

流程：

1. PC发命令->U1前置检查->连接，擦除，写入，运行->PC收到ACK

```text
  PC 发 CMD 0x0112 (PROGRAM)
    │
    ▼
  U1 前置检查 (app.c:673-693)
    ├─ 读 SPI Flash Slot0 头部 → size0, flag0
    ├─ 读 SPI Flash Slot1 头部 → size1, flag1
    ├─ size=0 或越界 → 0x06 ERR_NO_FW
    └─ flag≠0xA5      → 0x07 ERR_INCOMPLETE
    │
    ▼  (进入非阻塞状态机 APP_ProgramStep)
    │
  case 0: ISP_Connect()          ← BOOT0/PA8 拉高 + RESET/PB15
  复位时序, 发 0x7F
  case 1: ISP_MassErase()        ← 全片擦除 (0x44 + 0xFFFF)
  case 2: 读 Slot0 → ISP_WriteMemory() → U4 0x08000000
  (Bootloader)
  case 3: 读 Slot1 → ISP_WriteMemory() → U4 0x08000000+len0  (App)
  case 4: ISP_Go(0x08000000)     ← 跳转运行
    │
    ▼
  PC 收到 ACK (0x00=OK / 0x04=烧录失败)
```

已完成项：
0\. 命令解析

1. SPI FLASH读取(`gd25q128.c`)
2. 双槽设计验证
3. ISP协议实现(连接，擦除，写入，运行)<--代码完成，待硬件验证
4. 固件上传
   未验证项：
5. 硬件连接，实物烧录

# 7/30

## 同时校验3芯片4路ADC值

方案：复用 CMD 0x0020，增加 LEN=2 模式。PC 一帧下发阈值，U1自动串联收集三芯片 ADC → 逐通道算 MaxDiff → 与阈值比对 → 64 字节ACK 一次返回全部结果。

异常解决：  现象: CH1/CH2 判定正确，CH3/CH4 始终乱码

根因: uint8\_t result\[64] 放栈上，编译器对 ADC\_ConsistencyCompare
写入 result\[56..63] 的优化异常导致后半段未实际写入
修复: 改为 static uint8\_t result\[64] 放入 .bss 段

人话：编译器优化导致栈上的变量未实际写入，导致后半段数据乱码。

# 7/31

## ADC校准

`ADC_Calib_Run(void)`

预期
3 个来回 × 每向 4 次 × 每次 100 步 = 24 次 ADC 采样
CW: 读4次 → CCW: 读4次 → CW: 读4次 → ... → 电机归位 → ACK

首测发现：U4离线导致校准每次都超时，替换为U1检测，结果电机转动异常，一次指令仅转动一次而不是像U4检测代码没去除时那样如预期自动多次转动。经排查，U4模块的读ADC写了超时+重试，引入了600ms的延迟使得电机有足够时间走完100步，新版没有延迟处理导致电机指令立即到达。

修复：加了延迟宏，每500ms一条0x0014CMD,发送24条CMD后拼接最终ACK。

ADC\_Calib\_Run 不是一次跑完，而是每轮 APP\_Run
都被调用一次，每次只做一小步：发指令 → 等回复 → 等500ms → 读ADC →
判极值 → 决策下一步。phase 变量记住"上次做到哪了"，static
变量让计数器和极值在两次调用之间不丢失。

# 8/4

## SN码构建

1. `sn_report.c/h`读U4基址UID->memcmp去重->组帧->发送
2. A5 5B 00 24 00 00 00 10 \[版本0x01]\[UID 12B]\[XOR校验]\[预留 0x0000] CRC16
3. 触发：每次program握手成功后。去重：last\_uid初始全0XFF,首板必传，同板重复烧录不重复上报。容错：UID读失败静默跳过，不中断烧录

## 模块抽取

`app.c`太臃肿，抽出了4个耦合低的模块：

1. PC-U1帧接收状态机(`app_frame.c/h`)
2. ADC校验(`adc_consistency.c/h`)
3. ADC校准(`adc_calib.c/h`)
4. 固件上传(`fw_upload.c/h`)
5. 共享内部服务声明，此为**新增**，方便函数内部常用头文件一次引用到位(`app_internal.h`)

# 8/5

## 测径精度校准

`diam_calib.c`
流程：U1收到命令->U4归零->电机开转->读U4实测直径->算误差->上报

## ADC极值校准返回数据修改

删除中间数据帧，只回复最终完整帧

# 8/8

## 烧录测试

GD25Q:slot0断电丢失，剔除仅采用slot1进行测试，slot1电荷有时也会丢失。
串口助手测UART2的发送能收到回复。用手按U4的boot和rest能正常进入bootloader。通过U1传没办法进入。

ADC校准依赖于先把固件烧录进去，话说莫工给的固件里面的内容是什么，能读取文档中的协议吗（可读取）？我的思路发命令通过USART2传到U4，U4固件接收到命令后依照指令返回ADC值，电机接收到校准指令后转动，最终比对U4的ADC校准值。

查FLASH的手册看看开头的地址是不是不能烧录，导致掉电丢失

# 8/9

## SN码问题解决

1. 长度字节 (N-1) 后缺校验字节 (N-1)^0xFF（AN3155要求）。修复：补发校验字节
2. G031 读内存无尾随 ACK，读完数据后等尾随 ACK超时 → 返回错误 → SN 不发。处理：改短超时（20ms），超时视为成功

## 自动进入bootloader

现能直接U1传命令进入bootloader，无需手动按boot和rest。

## 双槽烧录问题解决

从单槽换回双槽掉电又不丢失了，找不回原先导致丢失的代码，当前代码可用。烧录真实固件，掉电重启仍可查询，满足要求

# 8/10

## 固件烧录后测试

1. 没办法触发正确协议。原因：之前代码中app写入地址是根据bootloader大小计算直接加在bootloader的后面，而实际bootloader和app固件的范围分别是start 0x08000000，size0x5000;start 0x08005000,size 0xA000，现在直接在代码中写入规定的地址。
2. 烧录验证：①成功查询到设置位置的向量表，表明两段都在设置范围内；②每字节发送都做校验，校验无误成功发送；③怀疑是U1引脚在烧录完后命令发不出去，测试接串口助手测试能测到帧命令；④串口助手接U4的TX，U1发送命令，U4的TX没有回应；⑤stm32cubeprogrammer查看U4芯片，发现固件被成功烧录进去，nBOOT0\_SEL=0（引脚模式）。引脚模式下 BOOT0 拉低 → 从主flash 启动，不是 bootloader；更换cube配置，强制从主flash启动，但是固件依旧不执行。⑥用cube软件直接查寄存器，发现上电时U4处于app运行区域，但是PC计数器一直卡着没变化，将寄存器值丢给AI解读->进入函数后无限循环卡死，从没抵达初始化UART，所以一直收不到报文流

**问题解决成功：**固件自身时钟初始化有问题，提交诉求后获得修正固件，烧录成功运行。

# 8/12

## U1的USART2协议改动

从8E1改到8N1，因为APP固件中是8N1，我们主动靠齐，但是bootloader仍然是8E1，因为这是官方协议规定的。

## 功能测试

1. PC发命令给U1去查看U4版本号->通过U1转发的命令，监听引脚确认U1已发出，U4不回复，一直在刷屏报文流；

做对照验证：不通过U1转发，直接通过PC-U4发送命令，依旧不回复，刷屏报文流。

**问题解决：**U4的CRC并非是文档的大端序而是小端序，修改CRC顺序即可（MODBUS RTU 序）。新创建帧创建函数`Frame_Build_1B_LE``Frame_Parse_1B_LE`给U4使用，确保CRC顺序正确。

2. 设置报文周期 成功；查看版本号 成功；
3. **测径精度校准** 
<br />

```C
  PC 发 0x0011
   │
   ▼
 【ZERO 归零】U1 发 0x10 给 U4 → U4 回 ACK（你从 COM4 看到 A5 5B 10...）
   │  归零失败 → 直接出结果全 FAIL
   ▼
 【MOVE 移动】U1 发指令给 U7：「电机转 206 步 CW」→ 电机物理转动
   │  等 U7 会话结束（不是等电机，只是等指令发完）
   ▼
 【WAIT 等待】死等 500ms → 让电机真正到位、停稳
   ▼
 【READ 读数】等 U4 发一帧新的 0x30 报文（最多等 1000ms）
   │  取报文里的 length（实测直径）= Dactual
   │  算误差 = |Dactual − 目标|
   │  误差 ≤ 50 → 这个位置 PASS，否则 FAIL
   ▼
 【NEXT】到下一个位置 → 回到 MOVE，重复 3 次
   ▼
 【DONE 汇总】拼 25 字节结果 → 作为 ACK 发给 PC
```
4. **ADC一致性校验**
当前是PC->U1，U1发送一条查询U4的ADC值指令（自拟的，未与固件方沟通），因此U4的ADC一直都是0。报文流中有U4的ADC值，将它提取出来作为校验值。
CH1:PB0
CH2:PB1
CH3:PA0
CH4:PA1

现在流程：
  读 U1 本地ADC → 从报文流取 U4 的 vo1/vo2/ams_adc → 等 U7 → 三芯片比对 → 回 64 字节

5. **ADC校准**
原本是读取U1的CH1,CH2，现更改为获取报文流数据读取U4的CH1,CH2。

# 8/18
## 代码优化
`adc_calib.c`/`diam_calib.c`：电机指令失败立即中止回错误码，不再"电机没动也照样采样"

U7 main.c：恢复 BSP_TMC_Init+SetDefaults（显式 16 细分，与 U1 测径换算 3200 步/圈对齐）+BSP_Motor_Init

大栈缓冲改 static 并互相复用（RAM 只有 10KB）：ACK/Event 合并、tx_data/rx_buf 命名 union、SEND/收帧/报文扫描复用会话缓冲，栈占用降 ~1.5KB

# 8/19
## 电机失步解决思路
静音模式容易发生软失步，启用 SpreadCycle（周期斩波模式），设置高IRUN
### 失步解决
1. 原配置 vsense=1 + irun=24 实际电流仅 ≈0.59A RMS，只有 1.5A 电机额定的 39%，力矩严重不足。改为 vsense=0 + irun=24 → ≈1.05A（70% 降额），力矩提升约 78%。同时 ihold 8→12，保证到位后有足够保持力矩锁死（防滑步）
2. 梯形加减速
3. 到位保持力矩，只停PWM，保持EN=0（TMC 以 IHOLD 电流锁住转子）
## 代码恢复
为了缩小体积而优化后的代码需额外增添才能恢复原功能，实测下来体积减少不了多少且有时原功能会失效，因此将U1代码回退到优化前的状态，但是在keil中额外启用精简c语言库，依旧缩减1kb。保留U7的电机代码更改。当前代码状况：命令正常收发，电机失步问题解决，余量告急，后期更换64KB芯片

# 8/22
## 电机回零+定点移动
U7启动打印影响了电机功能，关闭打印功能后，电机功能正常。
```text
加减速算法：
上层调用 BSP_Motor_Move(dir, steps, freq)
  │
  ├─► [模块 1] 将目标频率转换为 ARR 域寄存器值 (f 与 ARR 成反比)
  ├─► [模块 2] 规划梯形/三角形加减速步长 (小步数自动对半平分防急停)
  ├─► [模块 4] 执行安全启动序列 (清 UIF 防虚计、复位 HAL 状态机、开 UIE 中断)
  │
  └─► 主线程立即返回 (非阻塞)
        │
        ▼ (交由后台定时器中断处理)
      [模块 3] 每步进入 ISR: 计步 -> 判断到位停机 -> 分档调节 ARR 升降速
```
## 电机回零+定点移动协议调用
U1 ↔ U7（板间协议）  0x29 回零	A5 5B 29 N N 00 00 CRC（无数据）	1B 状态：0x00 OK / 0xFF ERROR
① 0x0012 回零（PC → U1 → U7 → 电机）

② 0x0013 定点移动（状态机，U1 主循环推进）  ： PC发0x0013 在命令匹配中设置状态，主循环中APP_RUN()每轮调用MotorSeq_Run()

0x0012 = PC 让 U1 替它发 1 条 0x29；0x0013 = PC 让 U1 替它发 4 条（1 条 0x29 + 3 条 0x23）。U1 的角色是"传话 + 记账"（motor_pos 累计位置），U7 是唯一"动手"的（转电机、检测限位）。

U7（执行者，4 文件）
bsp_motor.h：回零参数宏（方向/速度/最大步数/启动保护/去抖）
bsp_motor.c：BSP_Motor_Home/HomeDone/HomeFail、motor_halt、ISR 限位检测
protocol.h/.c：PROTO_CMD_HOME 0x29 + HandleHome
gpio.c：PB9/PB10 输入上拉

U1（转发+状态机，4 文件）
app.h：MGR_CMD_MOTOR_HOME 0x0012 / MGR_CMD_MOTOR_SEQ 0x0013
app.c：motor_pos 位置累计、0x0012 deferred、MotorSeq_Start/Run 状态机
protocol_u7.h/.c：U7_CMD_HOME 0x29 + U7_Home()