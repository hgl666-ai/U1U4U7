# 7/21

## TIM3 独立验证

TIM3正确计数

## 尝试读回复TMC2209

自环回通过，在关中断中进行收发，无回复。 **TODO**:发送指令操作电机运作，若电机不动，则说明TMC2209没收到数据帧

# 7/22

## TMC2209测试

读写都成功，过滤毛刺再读取不然没有读回复，芯片的8号5vout引脚电压不稳定，5v时才能正常读写，待芯片修复(7/23已修复，稳定5v);TMC2209要读写要放在程序最开始，不然很难抓到信号与之通信。地址是00

## TODO:烧录时给U4生成唯一的SN码

### 思路

用U4的UID做处理当SN码。每块U4编程时读自己的UID再写回去

### 过程构思

U4版放到治具上->U1 ISP读U4的UID地址（固定）->拿到UID->U1 ISP写UID 到 FLASH末尾待定区域（不占用固件空间）->烧固件

# 7/23

## 电机测试

### 遗留问题：HAL_TIM_PWM_Start_IT 不设 UIE

需要手动补 TIM3->DIER |= TIM_DIER_UIE 才能工作。可能是 TIM3 被
tim3_save/tim3_restore 接管后 HAL 内部状态与硬件不一致。后续可以在
tim3_restore 里加上 htim3.Instance->DIER 同步修复。

## 测电机相关的引脚电压

TMC2209的22引脚vs->24V;

(u7)PA5上电时变成0v，稳定后为3.3v;

(U7)PB4(MCPWM_DIR2):0V 和3.3V PB5(MCPWM_STEP2):交流档测上电时电压一直在变化，最高到达1.9V

### 硬件有问题，已修复

修复前功率仅0.4w，电流进不去电机，光响不转。修复后电机可转动，功率可达5W，顺逆时针正常转动

## 电机总结

电机正常转动，TMC2209温度在电机运作时50℃。

### 待优化：

```
HAL_TIM_PWM_Start_IT 漏设 UIE, 需手动 TIM3->DIER |= TIM_DIER_UIE;->  STEP 每发一个脉冲，电机走一步。TIM3 负责数步数——走到目标步数就停机。数步靠的是"更新中断"——脉冲结束时 CPU 被唤醒去 +1。HAL_TIM_PWM_Start_IT 本应自动开这个中断，但它没开。因为中间我们接管过 TIM3（给 TMC2209 做 UART），还回去后 HAL状态乱了。表现就是：电机不会自己停，超时报错。加 TIM3->DIER |= TIM_DIER_UIE 手动把中断开了就好。
```

## USB CDC修复

插上识别不出端口。7/25修复，F103系列不自带上拉电阻配给D+，添加后正常识别。

# 7/25

# PC下载固件到 SPI FLASH 设计思路

## 整体架构

**app.c**帧接收->APP_dispatch()命令路由->FW_START/DATA/END三段式上传处理->UploadData()缓冲-攒页-写FLash **|** ->**gd25q128.c**->擦除->写入

## PC传固件设置为三段式

固件大，一帧无法传完需拆成多帧。因此需要知道什么时候开始START(0X0009),数据DATA*N(0X000A),结束END(0X000B)。无START则不知写到哪里，有多大。无END则不知是否传完，残页没法刷，字节数无法比对。

## 状态机设计
**app_state:**
```text
IDLE->[START]->UPLOADING->[DATA....DATA]->END->IDLE
　　　　　　　　　　|__10s超时->IDLE
　　　　　　　　　　|__溢出->锁存错误(继续收ACK)
　　　　　　　　　　|__Flash写入失败->IDLE
```
## 双槽设计

烧录：
slot0:Bootloader 0x00000 64kb ->isp write->u4 0x08000000
slot1:APP 0x10000 64kb ->isp write->u4 0x08000000 + slot0_size
在FW_END后切换0/1，当两槽都有完整固件(判断长度及完整标志位flag 0xA5)时才能进行烧录，否则返回错误码。

## 固件下载边缘检测

| 测试编号 | 边缘检测点 | 代码位置 | 实现 |
| :--- | :--- | :--- | :---: |
| [1] | 正常 START | app.c:489 写头+切状态 | ✅ |
| [2] | START BUSY | app.c:442 state!=IDLE →<br>0x01 | ✅ |
| [3] | START LEN=6 | app.c:445 len<9 → 0x02 | ✅ |
| [4] | START size=0 | app.c:451 bytes==0 → 0x02 | ✅ |
| [5] | START size超限 | app.c:451 bytes>65528 →<br>0x02 | ✅ |
| [6] | START frames=0 | app.c:451 frames==0 →<br>0x02 | ✅ |
| [7] | 正常 DATA | app.c:506 UploadData() | ✅ |
| [8] | DATA BUSY | app.c:497 state!=UPLOADING →<br>0x01 | ✅ |
| [9] | DATA OVERFLOW | app.c:217 count+len>total →<br>0x0D | ✅ |
| [10] | DATA TIMEOUT | app.c:750 >10s → 0x0C | ✅ |
| [11] | 错误锁存 | app.c:500-502 upload_error→<br>直接回 | ✅ |
| [12] | 残页写入 | app.c:530-532 page_idx>0 →<br>刷入 | ✅ |
| [13] | 正常 END | app.c:519-554 完整流程 | ✅ |
| [14] | END BUSY | app.c:520 state!=UPLOADING →<br>0x01 | ✅ |
| [15] | END ERR_COUNT | app.c:538 count≠total →<br>0x0A | ✅ |
| [16] | 正常 QUERY | app.c:563-587 读头+回版本 | ✅ |
| [17] | QUERY NO_FW | app.c:575 size==0 → 0x06 | ✅ |
| [18] | QUERY INCOMPLETE | app.c:578 flag≠0xA5 →<br>0x07 | ✅ |


## PC下载固件到U1报错边缘检测

进行了18项测试用例，包括busy状态、overflow、timeout、error lock(某帧DATA出错，设置错误码永久锁住，后续DATA帧全返回锁存错误，防止后续DATA状态正常导致状态码被覆盖，新一轮上传或复位U1时恢复锁存状态)、error count、无固件、文件大小异常。

## u1->spi flash

第一轮：FW_START->DATA->END->写入Slot0(bootloader,0x00000)->ACK 0X00->upload_slot 自动切到1

第二轮：FW_START->DATA->END->写入Slot1(app,0x10000)->ACK 0X00->upload_slot 自动切到0

即发两轮指令，第一轮写入Slot0，第二轮写入Slot1，自动切换到0。

# 7/27

## 上位机软件+U1测试

新增转发查询U4版本号协议(PC->U1,U1->U4)，上位机协议向我对齐

# 7/28

## U1U7U4,4路ADC检测

添加协议，PC->U1可转发协议，查询U1U7U4的ADC值

## 测U4ADC值问题修复

U1与U7通讯不顺畅，U7的RX接收不到U1发送的指令。软件层面没问题，操作时忘记用跳帽将U1U7的RXTX短接。

# 7/29

## 读取flash固件烧录到测径板
**注意： 本ISP协议基于AN2606协议编写**

流程：
1. PC发命令->U1前置检查->连接，擦除，写入，运行->PC收到ACK
```text
  PC 发 CMD 0x0112 (PROGRAM)
    │
    ▼
  U1 前置检查 (app.c:673-693)
    ├─ 读 SPI Flash Slot0 头部 → size0, flag0
    ├─ 读 SPI Flash Slot1 头部 → size1, flag1
    ├─ size=0 或越界 → 0x06 ERR_NO_FW
    └─ flag≠0xA5      → 0x07 ERR_INCOMPLETE
    │
    ▼  (进入非阻塞状态机 APP_ProgramStep)
    │
  case 0: ISP_Connect()          ← BOOT0/PA8 拉高 + RESET/PB15
  复位时序, 发 0x7F
  case 1: ISP_MassErase()        ← 全片擦除 (0x44 + 0xFFFF)
  case 2: 读 Slot0 → ISP_WriteMemory() → U4 0x08000000
  (Bootloader)
  case 3: 读 Slot1 → ISP_WriteMemory() → U4 0x08000000+len0  (App)
  case 4: ISP_Go(0x08000000)     ← 跳转运行
    │
    ▼
  PC 收到 ACK (0x00=OK / 0x04=烧录失败)
```
已完成项：
0. 命令解析
1. SPI FLASH读取(`gd25q128.c`)
2. 双槽设计验证
3. ISP协议实现(连接，擦除，写入，运行)<--代码完成，待硬件验证
4. 固件上传
未验证项：
1. 硬件连接，实物烧录
# 7/30
## 同时校验3芯片4路ADC值
方案：复用 CMD 0x0020，增加 LEN=2 模式。PC 一帧下发阈值，U1自动串联收集三芯片 ADC → 逐通道算 MaxDiff → 与阈值比对 → 64 字节ACK 一次返回全部结果。

异常解决：  现象: CH1/CH2 判定正确，CH3/CH4 始终乱码

  根因: uint8_t result[64] 放栈上，编译器对 ADC_ConsistencyCompare
  写入 result[56..63] 的优化异常导致后半段未实际写入
  修复: 改为 static uint8_t result[64] 放入 .bss 段

  人话：编译器优化导致栈上的变量未实际写入，导致后半段数据乱码。

# 7/31
## ADC校准
`ADC_Calib_Run(void)`

预期
  3 个来回 × 每向 4 次 × 每次 100 步 = 24 次 ADC 采样
    CW: 读4次 → CCW: 读4次 → CW: 读4次 → ... → 电机归位 → ACK

首测发现：U4离线导致校准每次都超时，替换为U1检测，结果电机转动异常，一次指令仅转动一次而不是像U4检测代码没去除时那样如预期自动多次转动。经排查，U4模块的读ADC写了超时+重试，引入了600ms的延迟使得电机有足够时间走完100步，新版没有延迟处理导致电机指令立即到达。

修复：加了延迟宏，每500ms一条0x0014CMD,发送24条CMD后拼接最终ACK。

  ADC_Calib_Run 不是一次跑完，而是每轮 APP_Run
  都被调用一次，每次只做一小步：发指令 → 等回复 → 等500ms → 读ADC →
  判极值 → 决策下一步。phase 变量记住"上次做到哪了"，static
  变量让计数器和极值在两次调用之间不丢失。

  # 8/4
  ## SN码构建
   1. `sn_report.c/h`读U4基址UID->memcmp去重->组帧->发送
   2. A5 5B 00 24 00 00 00 10 [版本0x01][UID 12B][XOR校验][预留 0x0000] CRC16
   3. 触发：每次program握手成功后。去重：last_uid初始全0XFF,首板必传，同板重复烧录不重复上报。容错：UID读失败静默跳过，不中断烧录
  ## 模块抽取
  `app.c`太臃肿，抽出了4个耦合低的模块：
  1. PC-U1帧接收状态机(`app_frame.c/h`)
  2. ADC校验(`adc_consistency.c/h`)
  3. ADC校准(`adc_calib.c/h`)
  4. 固件上传(`fw_upload.c/h`)
  5. 共享内部服务声明，此为**新增**，方便函数内部常用头文件一次引用到位(`app_internal.h`)

  # 8/5
  ## 测径精度校准
  `diam_calib.c`
  流程：U1收到命令->U4归零->电机开转->读U4实测直径->算误差->上报
  ## ADC极值校准返回数据修改
  删除中间数据帧，只回复最终完整帧