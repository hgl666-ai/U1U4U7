# 2026-08-17

## U1-U7 治具系统代码全面审查（只读，未改代码）
- 依据《信息展示/项目总结_供代码审查.md》审查了 U1 全部应用层 + U7 全部应用层源码。
- 产出《信息展示/代码审查报告_2026-08-17.md》：严重 1 项、中等 11 项、轻微 10 项，总体评级 B。
- 关键发现：
  - 🔴 app_frame.c PC 帧接收状态机 frame_ready 后不拒收新字节，frame_pos 无上限 → 静态缓冲区溢出（274B）。
  - 🟡 U4/U7 单会话多消费者交叉消费响应（app.c 3s 自动 U7_PING、deferred 查询、一致性校验、校准共用）；校准忽略 MotorStep 错误；U4 报文无新鲜度校验；U7 main.c 中 BSP_TMC_Init/BSP_Motor_Init 被注释（细分假设 1600 vs 3200 三处不一致）；U7 协议通道残留 'P'/'G'/'F' 调试字节。
- 用户规则：未经允许禁止改动代码（本次审查严格遵守）。

## 修复实施（用户批准后）
- 已修 S1/M2/M4/M5/M6/M7/M9/M11 + 注释清理，明细见报告《信息展示/代码审查报告_2026-08-17.md》第八节。
- 关键决策：
  - IWDG ~2s（bsp_wdg.h 纯 inline 头文件，不改 Keil 工程）；喂狗点含 ISP_WaitForACK / ISP_ReadMemory / GD25Q_WaitForWriteEnd 循环内 → 烧录期间不误复位；数据传输走 DMA 不受影响。
  - U7 恢复 BSP_TMC_Init+IsAlive+SetDefaults（备份 main.c.bak 原有顺序），CHOPCONF mres=4=16 细分，与 diam_calib 3200 步/圈一致。
  - U1 U7_MotorStep 禁自动重试（非幂等）；U7 BSP_Motor_Move 改返回 1/0，busy 回错误。
  - U7 调试字节统一 #ifdef U7_DEBUG（默认关）。
- 未修：M1/M3/M8/M10，待后续。
- 注意：未编译验证（Keil 工程在用户侧），已按 C90 声明风格核对。

## RAM 溢出修复（2026-08-18）
- 用户编译报 L6406E（U1 .bss 超 0x5B0，F103C6 仅 10KB）：M9 静态化把栈上缓冲搬进 .bss 导致。
- 解法：静态缓冲互相复用（主循环单线程，生命周期互斥）——app ACK/Event 合并；u4/u7 tx_data 与 rx_buf 改命名 union `io`；u4 SEND frame 与报文解析合并 `u4_tmp`；u4 idle_frame_buf 复用会话 frame_buf；u7 SEND/Reset 复用 frame_buf；UART_BUF_SIZE 128→64。
- 净效果：.bss 比修复前少 ~120B、栈再降 ~1KB → 链接必过。
- 教训：F103C6 只有 10KB RAM，给"改 static 减栈"的方案要同步做缓冲复用，否则直接爆 RAM。

## Flash 溢出修复（2026-08-18 09:49）
- 用户编译报 L6220E：LR_IROM1 32772B 超 32768B（代码 32544 + RW 初始镜像 228）。
- 修复：删除 adc_calib.c M2 错误分支的诊断打印（错误码已走 ACK 回传），省 ~44B → LR ~32728B。
- 余量仅 ~40B：后续若再加功能，可开 AC6 LTO 或移除 app.c 两个 [WARN] 打印。
- U1 空间极限经验：32KB Flash + 10KB RAM 双紧，任何新增先算空间。

## 实测全挂排查（2026-08-18 11:22）
- 症状：命令全无回复、一键测试 0 通过、烧录超时、ADC 校验卡住（系统性故障）。
- 定位：最可能 U1 卡死在 `BSP_WDG_Init` 的 `while (IWDG->SR & 0x3U)`（USB 已枚举在前 → COM 存在；APP_Run 在后 → 永不到达 → 全无响应，症状全吻合）。
- 修复 1（两板 bsp_wdg.h）：影子寄存器等待改分步（PR→等 PVU→RLR→等 RVU）+ 每步 100/200ms 超时兜底，溢出周期 2s→4s（RLR 2499）。
- 修复 2（顺带发现的真 bug）：union io 的 tx 污染——带数据命令（U4 SetOffset/SetReportPeriod、U7 MotorStop/ReadInput）响应超时重试时 tx 已被 rx 覆盖 → 重发帧内容错误。已对这些命令设 max_retry=1 禁重试（零数据命令重试无污染）。
- 待用户验证：重烧两板后，先发 0x0111 PING 确认 U1 存活（回帧=U1 活着，问题在 U4/U7 链路），看 RGB 蓝灯慢闪=主循环正常。

## 11:40 用户实测反馈（U1 已活，U1↔U7 无响应）
- 证据：①0x0010 极值校准回 1 字节错误 = MotorStep 失败(M2 正确中止)；②一致性 U7 区 16B 全 0 连通道 ID 都是 0 = U1 未收到 U7 响应；③0x0011 测径 d 全 0 电机不动。U4 报文流正常（一致性里 U4 vo1/vo2/ams 有值）。
- 结论：U1↔U7 通信无响应。最可能 U7 未重烧最新固件（上一轮只有 U1 编译信息，U7 编译/烧录状态未知）。
- 已加排障开关：U7 main.c 定义 `U7_SKIP_TMC_CFG` 可跳过 TMC 初始化（排除 M6 嫌疑）。
- 待用户：确认 U7 重烧 → 发 0x0008 查 U7 版本 / 0x0114 TEST_U7 定位。

## 13:52 根因确认：IWDG 导致电机不动（★重要）
- 用户实测链条：注释 U7 WDG → U1↔U7 通信恢复（ADC 能读）；再注释 U1 WDG；克隆审查前版本（无 WDG）重烧功能完善；**最终：注释当前项目看门狗后电机能动了**。
- 根因：IWDG 导致 U7 反复复位/卡死 → MotorStep 执行被打断 → 电机不动。对照克隆版（审查前，无看门狗）确认。
- 修复：两板 `bsp_wdg.h` 加 `BSP_WDG_ENABLED` 总开关，**默认关闭**（Init/Feed 编译为空操作，零代码开销，顺带省 Flash）。启用需先解决 U7 复位机制。
- 教训：给电机实时控制板加看门狗前必须实测喂狗可靠性；此项目 IWDG 与硬件组合不兼容，宁可不加。

## 13:56 用户决定：彻底删除看门狗
- 已删除 U1/U7 的 `bsp_wdg.h` 文件（用户已手动删，代码引用已全部清除）。
- 清除的引用：U1 main.c（include+Init）、app.c（include+Feed）、isp_programmer.c（include+2×Feed）、gd25q128.c（include+Feed）、U7 main.c（include+注释掉的 Init/Feed）。
- 保留：U1 `stm32f1xx_hal_conf.h` 的 HAL_IWDG_MODULE_ENABLED（注释态，CubeMX 生成，无影响）。
- 状态：看门狗功能从代码中完全移除，回归为无 WDG 方案。

## 14:12 上位机联动问题 + 校准 Enable 修复
- 用户反馈：点 0x0112 烧录后上位机自动跟了一条 0x0010 极值校准并返回结果，电机没动。预期"点哪条跑哪条"。
- 澄清：U1 固件在 0x0112 后不会自动发起任何命令，跟的是上位机自动流程 → 改上位机。
- 发现真问题：ADC_Calib_Start / DiamCalib_Start **缺少 BSP_U7_Enable()**（TEST 流程 case 17 有），若 U7_EN 参与供电则校准电机必不动。已给两个 Start 补上 BSP_U7_Enable()（对齐 TEST，USB 直供则无害）。
- 待用户：确认 0x0010 返回 8B(成功)还是 1B(错误码)；重烧后验证。

## 16:06 结论：U7_ENABLE 已还原
- 用户确认 U7 为 USB 直供电，U7_EN 不参与供电 → 已还原 adc_calib.c / diam_calib.c 的 BSP_U7_Enable() 及 include。
- 用户实测：所有命令测试通过 ✓。
- ★项目硬事实：U7 供电 = USB 直供，U7_EN 仅作使能信号（TEST 流程保留 BSP_U7_Enable 为历史习惯，不影响校准）。

## 08-19 电机失步修复（梯形加减速 + 电流修正 + 保持力矩）
- 根因（结合原理图）：①TMC2209 电流不足——vsense=1+irun=24 → ~0.59A RMS = 1.5A 电机的 39% → 力矩不足全程跳步；②无加减速硬起硬停；③到位即脱机失去保持力矩。SPREAD 引脚=GND 锁定 StealthChop2（寄存器改无效）；Rsense=110mΩ 外部；VREF 固定 0.833V。
- 修改（U7 3 文件，已批准实施）：
  - `bsp_motor.h`：新增加减速宏（F_START=500Hz/ACCEL=800/DECEL=800/GRAN=2/MIN=4/FREQ_MAX=10k）
  - `bsp_motor.c`：梯形加减速 ISR 状态机（ARR 域线性、三角退化、防除零）；安全启停序列（CNT 清零→EGR.UG→清 UIF→清 NVIC 挂起→EN=0→Start_IT→busy 最后置位）；到位只停 PWM 保持 EN=0（BSP_Motor_Stop 唯一脱机入口）；BSP_Motor_Init 运行时置 ARPE+OC2PE
  - `bsp_tmc2209.c` SetDefaults：vsense 1→0、irun 24→28（≈1.21A, 80% 降额）、ihold 12→10
- 注意：①校准小步数（100 步）走三角退化低速，耗时略增但 U1 等待足够；②电机到位后保持力矩通电（~0.46A），校准完不脱机——U1 无 MotorStop 调用，需留意发热（后续可加空闲超时脱机）。
- 待用户：Keil 编译 U7 → 0x0010 验证平滑转动/0x0011 定位/正反转 20 次/温升。

## 08-19 13:40 编译错误 + 电机不转发热 → 二次修正
- ①编译错误：`TIM_CCMR2_OC2PE` 不存在——CH2 的 OC2PE 位在 **CCMR1 bit11**（用户手动改 CCMR1 正确）。
- ②电机不转 + TMC 发热：弃用预装载方案（ARPE/OC2PE 已从 BSP_Motor_Init 移除，恢复 ARPE=0 立即生效模式——ISR 入口 CNT≈0 写 ARR 无截断，加减速安全）；TMC 电流保守化 **vsense=0 + irun=24（≈1.05A, 70% 降额）+ ihold=8**（irun=28 实测疑似触发过流/过热保护）。
- 教训：改电流参数必须小步验证（24→26→28），不能一步到位；预装载寄存器位定义要查参考手册（CH2→CCMR1）。

## 08-19 14:05 电机"只转一下+锁死" → 发现减速段 uint16 溢出 bug
- 现象：0x0010 电机只转一下，电源 15W（≈0.63A = IHOLD 保持电流），电机锁死掰不动（驱动/保持力矩正常 → 问题在 STEP/计步）。
- 真 bug：ISR 减速段 `ramp_arr`(uint16) 每 2 步 +1920，第 4 档 `64839+1920=66759` 溢出为 1223 → ARR 突变 26kHz → 减速轨迹错乱、剩余步高速狂转（"转一下"）。
- 修复：减速 clamp 改为"先判后加"（`(uint32_t)ramp_arr+step_d >= arr_start` 则 clamp，否则相加）。
- 附加：U7 protocol.c HandleMotorStep 加 U7_DEBUG 拒绝原因打印（STEP reject: len<4/motor/step=0/BUSY + STEP ok steps=）。
- 待用户：重烧 U7 后 0x0010，报告 ACK 内容（几字节/错误码或 8B 结果）+ 电机转动情况；若仍失败可开 U7_DEBUG 用 USB-TTL 监听 U7 PA9。

## 08-19 14:25 "依旧只转一下+锁死" → 根因：HAL State 卡 BUSY（决定性修复）
- 用户用旧固件（克隆版）验证：电机能随意转动（仅失步）→ **排除 TMC/电流/硬件**，问题锁定在 bsp_motor.c。
- 真根因：新 ISR 到位分支**直接寄存器停 PWM**（DIER/CCER/CR1），未走 HAL → `htim3.State` 停留 `HAL_TIM_STATE_BUSY`（Start_IT 置的）→ 之后每次 `HAL_TIM_PWM_Start_IT` 开头 `if (State != READY) return HAL_BUSY` **静默拒绝启动** → 校准第 2 段起电机不动，但 BSP_Motor_Move 未查返回值仍回 OK → U1 假完成返回 8B 结果 → 表象"只转一下+锁死"。
- 旧固件为何正常：ISR 走 `HAL_TIM_PWM_Stop_IT` → State 回 READY → 每段可重启。**唯一差异**。
- 修复（bsp_motor.c 两处）：①ISR 到位分支加 `htim3.State = HAL_TIM_STATE_READY`（与旧版 Stop_IT 行为等价）；②BSP_Motor_Move 检查 Start_IT 返回值，非 OK → EN 拉高+busy 清+return 0（防假启动）。
- 补充事实：0x0010 每段 100 步=11.25°≈0.19s("转一下"是数学必然)；完整流程 24 段（3 往复×4 采样×2 方向）≈16s；到位保持力矩(EN=0+IHOLD)→掰不动是**新设计行为**，旧版到位脱机(EN=1)才能掰动。
- 待用户：重烧 U7 → 0x0010 应连续多段转动约 16s → 8B 结果；0x0011 定位；确认"掰不动"是否可接受（否则校准完加 MotorStop 脱机）。

## 08-19 15:39~15:55 抓包定位"第二段 ERROR + 不锁死"（手册 Rev1.03 核对版）
- 抓包铁证：第一段 MOTOR_STEP **OK**（电机转 100 步）；500ms 后第二段 **ERROR(0xFF)** → U1 回 0x05。现象：转一下、停止、**不锁死**。
- ★★**纠错**：15:39 轮误判"IHOLD_IRUN 位域错位"——经用户提供手册 **Rev1.03 p25 核实：TMC2209 的 IHOLD_IRUN 布局是 IHOLD=bit4:0(5)、IRUN=bit12:8(5)、IHOLDDELAY=bit19:16(4)（中间各留 3 位 reserved），与 TMC2208 不同！原代码位域本来就正确，已回退**。教训：位域必须按芯片手册核对，不能套用同系列其他型号。
- 手册其他确认：①FREEWHEEL(bit21:20) 仅在 **I_HOLD=0** 时生效（p32）→ freewheel=1 无害；②IRUN 上电默认=31（p25）→ 旧固件靠 VREF 默认电流能转。
- "不锁死"确定根因：**IHOLD=8 在 vsense=0 下仅 ≈0.35A，对 1.5A 电机保持力矩不足**（14:25 的"锁死 15W"是 IRUN 保持，非 IHOLD）。修复：**ihold 8→12（≈0.5A）**；freewheel 1→0 规范化为 normal。
- "第二段 ERROR"（busy 卡 1）：代码逻辑（ISR/加减速/HAL State/无 TMC 中途调用）全部排除，**待 U7_DEBUG 实证**：已加 ISR 到位打印 `MOVE done`；main.c U7_DEBUG 下上电自动跑 `BSP_TMC_Test()`（通信/写读回/故障标志全套）；ReadReg [DBG] 打印包进 U7_DEBUG 防污染协议流。
- 待用户：先重烧（默认关 U7_DEBUG）验证锁死+多段；若第二段仍 ERROR → 开 U7_DEBUG 重烧，USB-TTL 监听 COM4，上电日志（TMC Test 结果）+ 0x0010 日志（STEP ok/MOVE done/MOVE reject）发我。

## 08-19 16:41 U7_DEBUG 铁证 → ★最终根因：HAL v1.10+ 的 ChannelState[]（通道级状态）
- U7_DEBUG 抓包（COM4 hex→ASCII）：`MOVE done (ISR reached target)`（第一段 ISR 正常走完 100 步）+ `MOVE reject: START_IT FAIL`（第二段）→ **第二段 HAL_TIM_PWM_Start_IT 返回失败**，而此前已强制 `htim3.State=READY` → 说明检查的不是 State！
- ★根因：本工程 HAL 是 **v1.10+ 新版**，`HAL_TIM_PWM_Start_IT` 用 `TIM_CHANNEL_STATE_GET(ChannelState[])`（**通道级状态数组**）检查，不匹配返回 **HAL_ERROR**（非旧版 HAL_BUSY）。第一段 Start_IT 置 `ChannelState[CH2]=BUSY`，ISR 直接寄存器停 PWM 未还原 → 第二段必失败。**只改 htim->State 无效（新 HAL 中 State 是兼容遗留字段）**。
- 修复（bsp_motor.c 两处）：①ISR 到位分支加 `TIM_CHANNEL_STATE_SET(&htim3, TIM_CHANNEL_2, HAL_TIM_CHANNEL_STATE_READY)` + `State=READY`；②Move Start_IT 前同样强制还原（双保险）。v1.10 Stop_IT 无条件执行+置 READY（无状态检查），BSP_Motor_Stop 安全。
- ★★通用教训：**新 HAL(1.10+) 用 ChannelState[4] 通道级状态，Start_IT 返回 HAL_ERROR；ISR 直操寄存器启停 TIM 必须手动同步 ChannelState + State 两个字段**（旧固件走 HAL Stop_IT 所以正常）。
- 抓包无 TMC Test 输出 → 用户固件可能未含 15:5x 的 main.c 诊断（不影响本次修复）；"MOVE done" 证明第一段电机有力走完 100 步 → SetDefaults/位域/电流正常，"转一下"=100步(11.25°)数学必然。
- 待用户：重烧 U7 → 0x0010 应连续 24 段约 16s 转完回 8B，到位锁死（ihold=12）。

## 08-19 17:0x 临时改动：0x0010 改"正转1圈+反转1圈"测失步（测完必须恢复）
- 原因：24 段每段 100 步(11.25°)肉眼难判断是否失步 → 用户要求临时改一整圈肉眼验证。
- ★临时修改 `U1/Core/Inc/app.h`（恢复值在注释里也写了）：
  - ADC_CALIB_STEPS_PER_READ: 100→**3200**(1圈@16微步)（恢复 100）
  - ADC_CALIB_READS_PER_DIR: 4→**1**（恢复 4）
  - ADC_CALIB_CYCLES: 3→**1**（恢复 3）→ 流程共 2 段：CW 1圈 + CCW 1圈
  - ADC_CALIB_MOTOR_DELAY_MS: 500→**3000**（3200步梯形加减速≈2.1s，需加长等待；恢复 500）
- 依赖确认：U7_MotorStep timeout=5000ms > 2.1s ✓；U7 协议 steps=uint16, 3200 OK ✓；U1 编译无新增代码量（宏常量）✓。
- 测试要点：观察电机是否完整转一整圈、回到起点（正转后反向应回原点）→ 判断有无失步；顺便验证到位锁死（ihold=12）。
- ★待用户测完 → 恢复 app.h 四个宏为 100/4/3/500，并去掉临时注释。

## 08-19 17:1x ★失步问题全部解决（用户确认"转动丝滑无失步"）
- 用户用"正转1圈+反转1圈"测试：**转动丝滑、无失步** → 失步问题闭环。
- 已恢复 app.h 为原 24 段（100/4/3/500）并去掉临时注释。
- 总结（为讲给用户）：①TMC 电流 vsense=1→0 + irun 24（≈0.59A→1.05A RMS，力矩提升 78%）+ ihold 8→12（到位锁死）；②梯形加减速（500Hz 起步/2kHz 目标/三角退化/安全启停序列）；③到位保持力矩不脱机（防滑步）；④HAL v1.10+ ChannelState[] 还原（多段连续运动的关键）；⑤16 微步与 U1 3200 步/圈对齐。后续关注点：U1 侧 24 段回归（0x0010 完整跑 ~16s）+ 0x0011 测径定位校准。

## 08-20 08:4x 上位机软件发命令卡住 → S1 拒收回归修复
- 现象：SSCOM 逐条发命令正常；上位机软件发命令卡住（等响应超时）；旧固件两者都正常。
- 排查：diff 新旧全命令路径（app_frame.c / app.c / usbd_cdc_if.c / bsp_uart.h）——**PC 帧接收路径唯一差异 = S1 的 `if (frame_ready) return;`**（app_frame.c）。上位机"同 USB 包/紧邻包连发多帧"时，第二帧字节在 frame_ready=1 期间被拒收丢弃 → 上位机等不到第二条的响应 → 卡。SSCOM 手动逐条发（帧间隔大）不触发。旧固件无拒收（字节被状态机吞进缓冲尾部）不丢。
- 修复（U1 两处，零 RAM 新增）：①app_frame.c 移除 `if (frame_ready) return;`（保留 frame_pos 硬上限防溢出）；②app.c APP_Run 在 Frame_Parse 成功后，把 frame_buf[used..] 残余字节（第二帧）经 data[] 临时区重放给状态机（memcpy 防覆盖，n 截断防 data 越界）。帧总长 = FRAME_OVERHEAD(10)+len+CRC(2)。
- 注意：U1 Flash 余量仅 ~40B，重放代码 ~30-60B，若 L6220E 超限 → 删 [WARN] 打印或开 LTO。
- 待用户：重编译 U1 → 上位机软件发命令验证；同时回归 SSCOM + 24 段 0x0010。

## 08-20 09:4x 上位机仍卡 → 全面向旧固件看齐（回退 S1 + FIFO + 重放）
- 用户反馈：重放方案后上位机仍卡；且指出"新固件体积超过旧固件，背离缩小初衷"。
- ★关键认识：**旧固件 app_frame 的 LEN 检查（frame_dlen>FRAME_MAX_DATA 即复位）已天然把帧长卡死在 8+256+2=266 < 274，frame_pos 根本不可能越界 → S1"溢出防护"是过度防御**，拒收反而引入上位机连发丢帧回归。
- 回退（U1 三处）：①app_frame.c 完全恢复旧固件状态机（取消 S1 拒收+硬上限）；②app.c 删除残余重放段（~60B）；③bsp_uart.h UART_BUF_SIZE 64→**128**（FIFO 溢出嫌疑：上位机连发时 U1 消费不及，64B 丢 U7 响应→卡；SSCOM 慢发不触发；旧固件 128 正常）。
- 体积对比（map）：重放版 Total ROM 32692B（LR 0x81A0 已超 0x8000 极限）；旧固件 32632B；恢复后 ≈32630B 退回 32KB 内。RAM：FIFO 128 后 RW≈10040B，10KB 上限余 ~200B（紧但可过）。
- 教训：**F103C6 32KB/10KB 双极限，任何防御性代码都要先算体积账，旧固件能跑就不过度设计**。
- 待用户：重编译 U1（全量）→ 上位机连发命令验证；若仍卡 → 需上位机发包方式（哪条命令/间隔）或 U1 USB 日志定位，不再盲改。

## 08-20 09:5x 卡死根因：U4 离线 + 校准命令空跑/脏数据 → U4 在线检测快速失败
- 用户定位：**U4 测径板没启动时**上位机发 0x0020/0x0010/0x0011 卡住；U4 启动后正常；SSCOM 无论 U4 状态都能返回。
- 分析：三条命令 U4 离线时 U1 **其实都返回**，但行为是：0x0011 U4_ZeroSensor 200ms 失败→DONE；0x0010 用默认 u4_report **空跑 24 段电机 16s**（上位机超时短→显示卡，SSCOM 无限等→能等到）；0x0020 用 U4 脏数据出结果（上位机等有效数据→卡）。SSCOM 无等待超时所以"都能返回"。
- 修复（U1）：新增 **U4 在线检测**——protocol_u4.c 记录 u4_report_tick（U4 报文最后到达时刻），U4_IsOnline()=tick 距今<500ms；app.c 的 0x0020/0x0010/0x0011 入口 U4 离线 → **立即回 APP_STATUS_ERR_TIMEOUT(0x0C)**，不空跑/不喂脏数据。U4 在线时行为不变。
- 体积：U4_IsOnline+3 处检查 ≈+65B；此前恢复 S1/重放已省 ~60B，净增很小。若 L6220E → 删 [WARN] 打印或开 LTO。
- 待用户：重编译 U1 → U4 不启动时上位机发三条命令应快速返回 0x0C（不卡）；U4 启动后正常跑。若上位机对 0x0C 仍卡 → 上位机软件对错误码的处理问题，需上位机配合（U1 已尽力）。

## 08-20 10:0x ★用户决定：U1 全面回退旧固件（git clone/2），U7 电机修复保留
- 背景：U1 侧 2 天改动（S1/M2/M4/M9/看门狗/FIFO/U4 在线检测等）引入上位机卡住 + 固件体积超旧固件，根因未完全定位；用户明确"照着旧固件恢复，电机部分保留并适配"。
- 操作：①备份当前 U1 → `U1/U1project_20260820_bak`；②删除 Core/Drivers/Middlewares/USB_DEVICE/ioc 后从 git clone 旧固件整体复制（**MDK-ARM 被 Keil 占用未删，但 uvprojx 忽略行尾符后与旧固件完全一致**，工具链均为 AC5 V5.06）；③diff -rq 验证 Core、USB_DEVICE 与旧固件**逐文件一致**（空输出）。
- ★适配确认（零改动）：U1 旧固件 `DIAM_CALIB_STEPS_PER_REV=3200`（注释"200步×16细分"）与 U7 新固件 mres=4（16 微步=3200 脉冲/圈）**天然匹配**——之前"测径不准"正是旧 U7 靠硬件默认 8 微步所致，现在 U7 显式 16 微步正好修正。U1↔U7 协议（0x21-0x28）未变，兼容。
- 保留的 U7 电机修复：梯形加减速、TMC 电流(vsense=0/irun24/ihold12)、到位保持力矩、HAL v1.10 ChannelState 还原、16 微步显式配置、busy 回错误。
- 待用户：关闭 Keil 重开 U1 工程（Keil 正占用 MDK-ARM）→ 全量 Rebuild → 回归全部命令（SSCOM + 上位机）。备份 `U1project_20260820_bak` 待确认后删除。

## 08-20 10:4x "仍依赖 U4 在线" → 根因：烧的 U1 固件是恢复前的（10:11 构建）
- 用户反馈：旧固件不论 U4 是否在线都能执行上位机；当前项目必须 U4 在线才能执行。要求审查旧固件解决。
- 审查结论：①恢复后的 U1（Core/USB_DEVICE）与 git clone 旧固件**逐文件一致**（diff 验证过）；②旧固件 U1 的 adc_calib 对 U7 错误不检查（照常继续）、U4 离线照常返回（0x0020 脏数据/0x0010 16s 空跑/0x0011 200ms 失败）→ 与 git clone 行为一致；③U7 差异（M7 busy ERROR、TMC 初始化、16 微步、U7_DEBUG 打印）均不影响"U4 依赖"；④**U7 uvprojx 无 U7_DEBUG 定义** ✓。
- ★根因：**U1 的 axf/hex 构建时间是 Aug 20 10:11 = 恢复操作(10:20)之前** → 用户烧的固件是"含 U4_IsOnline 快速失败(10:07 加)"的版本，U4 离线时命令立即回 0x0C → 上位机处理错误卡住 → 表现"依赖 U4 在线"。恢复后的 U1 源码未编译。
- 解决：用户重编译恢复后的 U1（全量 Rebuild）→ 行为应回到旧固件（U4 离线照常返回）。
- 待用户：重编译烧录恢复后 U1 → 验证；若仍有问题提供具体命令/现象。

## 08-20 11:0x 恢复后验证通过 + U7 ihold 12→8（降保持功耗）
- 用户确认：**恢复后 U4 不在线也能运行上位机软件** → 问题彻底解决（根因确为烧了恢复前 10:11 的固件）。
- 回答用户：U7 梯形加减速全保留（U1/U7 是独立工程，只恢复了 U1，U7 未动；串口协议 0x21-0x28 未变 → 天然兼容）。
- U7 ihold 12→8：实测保持功耗 10W（ihold=12≈0.5A）怕发热，测量治具轻载 → 降回 8（≈0.35A≈7W）。备注：还热可 6，或上位机流程结束发 0x24 MOTOR_STOP 脱机(0W)。
- 待用户：重烧 U7 验证保持功耗与锁死（ihold=8 若掰得动再回 10~12）。

## 08-20 13:5x 新增电机回零 + 定点移动（0x0012/0x0013, 已实施待编译验证）
- 需求：IN1(PB9)=零点、IN2(PB10)=对侧限位（均低电平触发）；回零命令→电机 CCW 反转找零点；定点移动序列：回零→1mm→1.5mm→2mm（换算 um×3200/15500：206/310/413 步）。
- 架构：回零逻辑在 U7（ISR 每步检测 IN1/IN2）；U1 仅转发+位置累计（motor_pos）。
- U7 改动：bsp_motor.h/.c（MOTOR_HOME_DIR=CCW/SPEED=500/MAX_STEPS=1600；BSP_Motor_Home/HomeDone/HomeFail；ISR 回零检测：IN1=home_done、IN2=home_fail、到最大步数=超时失败；抽 motor_halt() 统一停 PWM+还原 ChannelState）；protocol.h/.c（PROTO_CMD_HOME 0x29 + HandleHome 阻塞≤4s 回 OK/ERROR）。
- U1 改动：app.h（MGR_CMD_MOTOR_HOME 0x0012 / MOTOR_SEQ 0x0013）；protocol_u7.h/.c（U7_CMD_HOME 0x29 + U7_Home 等 1B/5s 超时/禁重试）；app.c（motor_pos + 0x0012 走 deferred 机制 + 0x0013 MotorSeq 状态机 HOME→MOVE→WAIT(500+delta*2ms)→DONE，PING 暂停条件加入 MotorSeq_IsBusy）。
- 注意：①C90 声明顺序已按 Keil AC5 修正；②回零期间 U7 阻塞 ≤3.2s。
- [2026-08-20 14:3x] U1 L6220E 超限 316B（33084 vs 32768）→ 已腾空间 ~470B：删 [WARN]×2 打印（SEQ mismatch/CRC error，错误行为不受影响，仅丢调试日志）；fw_upload [ERROR] 打印（错误码经 ACK 回传）；启动横幅 3 行→1 行；TEST 流程 10 处 PASS/FAIL 重复块合并为 APP_TestResult(ok) 共用函数（输出行为完全一致）。
- [2026-08-20 16:1x] ★回零实测：0x0012 电机 CCW 走满 1600 步(半圈)从未触发限位 → U7 超时失败 → U1 回 0x0C；0x0013 卡在回零阶段失败回 0x05。**根因：限位触发电平判断反了**——计划阶段确认"触发变高"，但 ISR 实现 `BSP_IN_Read()==0`（低电平触发），GPIO NOPULL+外部下拉 触发时读 1 → 永远检测不到。已改 `!=0`（高电平触发），同步修正 bsp_motor.c ISR/头注释 + 命令大全 0x29 描述。
- [2026-08-20 16:2x] 修复后再测：电机"不动但有力"（保持力矩在、STEP 没走）+ 间歇性（成功过一次三段式）。**三处加固**：①gpio.c PB9/PB10 NOPULL → GPIO_PULLDOWN（防悬空读值漂移导致回零 ISR 第一拍误触发 home_fail → 表现为"不动但有力"、0x0013 回 0x05）；②HandleHome 兜底超时后强制 BSP_Motor_Stop()（防 busy 残留卡死后续命令）；③app.c PING 暂停条件加 deferred_cmd==0（0x0012 回零走 deferred，MotorSeq_IsBusy 为 0 时 PING 仍会发，与 U7 单会话竞争）。
- 待用户：重编译 U7（gpio.c+protocol.c）+ U1（app.c）→ 重测 0x0012 / 0x0013；若仍不动，开 U7_DEBUG 抓 MOVE reject 日志。
- [2026-08-20 17:2x] ★用户偏好：调试开关用 `#if U7_DEBUG` / `#endif` 风格（不用 #ifdef，不用 Keil Define），总开关在 `bsp_tmc2209.h:42 #define U7_DEBUG 1`（已启用）；已把 bsp_motor.c/protocol.c/main.c 全部 `#ifdef` 改 `#if`（15 处），protocol.c 补 include bsp_tmc2209.h。用户风格：0=关 1=开，改头文件即可。
- [2026-08-20 17:3x] ★★触发极性最终修正（U7_DEBUG 实证）：日志 `HOME done (IN1)` + 电机不转 = **未触发时 IN1 读 1（硬件外部上拉+开关拉地）→ 触发=低电平**。此前按计划确认"触发变高"写成 !=0 是错的（未触发即误判，电机不动却回零成功）。已改：bsp_motor.c ISR `==0` 低电平触发（保留启动保护+去抖）；gpio.c PULLDOWN→**PULLUP**；bsp_motor.h/命令大全注释同步。
- [2026-08-21 09:0x] ★★回零方向实测修正：用户连续 3 次 0x0012 + 3 次 0x0013，电机每次 **CCW 转满 1600 步（半圈）从未触发 IN1/IN2**（B→A→B 位置变化=电机在转）→ 开关不在 CCW 行程内 → **零点在 CW 侧**。已改 `bsp_motor.h MOTOR_HOME_DIR: CCW→CW`（极性 `==0`+PULLUP 保持正确）。
- [2026-08-21 09:3x] ★★ISR 阻塞打印修复（COM4 实证）：`MOVE done (ISR reached target)` + U7 3.2s 回 0xFF 帧，但 U1 5s 超时（收不到回帧）→ **ISR 内 MOVE_DBG/HOME done/HOME fail 打印阻塞 USART ~3ms，回零 500Hz=2ms/脉冲 → 丢脉冲卡顿 + 干扰响应帧**。已把 ISR 内 3 处打印全部移除，状态打印移到 HandleHome 主循环（U7_DEBUG 下）。
- [2026-08-21 14:3x] ★★★TMC 微步漂移最终根因 + 修复：用户 14:12 连续测试回零/定点：插 GND 能停（回零逻辑 OK）、不插 GND 超时（预期）、但**"3200 步有时=2 圈有时=1 圈" + 中途卡顿 + 断电重启后第一次测试失败**。结合历史（09:22 时 3200 步=1 圈=16 微步；14:12 时 3200 步=2 圈=8 微步）→ **TMC SetDefaults 写入间歇失败且无读回验证 → mstep_reg_select/mres 未生效 → 微步在 16/8 之间漂移**（MS1/MS2=00 硬件默认 8 微步）。已修复（U7 3 文件）：
  - `bsp_tmc2209.c`：新增 `BSP_TMC_WriteRegVerified`（写+读回对比+重试 3 次）；`SetDefaults` 改返回 uint8_t，5 个寄存器全部读回验证；补 ReadReg 前向声明（C90）
  - `main.c`：IsAlive/SetDefaults 失败整轮重试（最多 3 轮，轮间 50ms）；U7_DEBUG 下上电打印 `TMC CFG OK (16ustep)` / `TMC CFG FAIL (8ustep default!)`
  - `bsp_tmc2209.h`：SetDefaults 返回类型 + WriteRegVerified 声明
  - 关键认知：TMC2209 写命令无响应，必须读回验证；GSTAT 等"写1清位"寄存器不适用验证
- 待用户：重烧 U7 → COM4 上电日志应见 `TMC CFG OK (16ustep)` → 数圈验证 3200 步=1 圈 → 0x0012/0x0013 回归。若仍 FAIL 则 TMC 通信硬件问题（接线/供电），需查上电 TMC Test 日志。
- [2026-08-21 15:0x] ★★用户上电日志（U7_DEBUG）逐字节 CRC 验证（Python）后的**决定性认知修正**：
  - **CHOPCONF=0x140100C3 / PWMCONF=0xC80D0E24 读回=期望值（16 微步已生效！用户实测"转一圈"=3200 步=1 圈证实）**
  - **IHOLD_IRUN/TPOWERDOWN 读回 0 是正常**（手册 p25 标注 **W 只写寄存器**！读回恒 0）→ 之前的 FAIL 是**误报**，IRUN=24≈1.05A 其实一直生效（电机有力证明）
  - **GCONF 读回 0x1D0 vs 期望 0x1D1 → 只缺 bit0（I_scale_analog）**；手册 p20：**0=内部基准(5VOUT)，1=VREF**（我们注释语义反了！）→ 该位硬件/OTP 锁定为 0，恰好 = 内部基准 = 我们想要的模式（电流由 IRUN 决定）
  - 修复：SetDefaults 验证策略改为——GCONF 写后**掩码验证**关键位（pdn_disable/mstep_reg_select/multistep_filt，不验 I_scale_analog）；IHOLD_IRUN/TPOWERDOWN 改普通 WriteReg（只写不验证）；CHOPCONF/PWMCONF 保持 WriteRegVerified；I_scale_analog 改写 0 + 注释修正
  - 待解问题：**定点移动回零后"不往前"**（重启后 5 次都转一圈但插 GND 不往前）——疑点：插 GND 时机/接触 or MOVE 链路，需重烧后开 U7_DEBUG 抓 0x0013 COM4 日志（HOME done? STEP ok/reject? 一锤定音）
- [2026-08-21 15:3x] ★验证进展 + 新修复：
  - **A 组（回零 0x0012）5 用例全过**；B3（回零失败中止 0x05）、B5（忙时拒绝 0x01）过；**B1（定点 0x0013 完整流程）时好时坏**——好时怎么发都能走完三段，坏时插 GND 回零停住后 MOVE 段不走
  - **新修复（U1 protocol_u7.c）**：`U7_MotorStep` / `U7_MotorStop` 补 `max_retry=1`——**M7"电机命令禁重试"在 U1 回退旧固件时丢失**（U7_RETRY_MAX 默认 3），超时重试会重复发 0x23 → 电机重复动作 → 开环位置过冲。U7_Home 已有 max_retry=1
  - PING 暂停条件（含 MotorSeq_IsBusy + deferred_cmd==0）与 U7_Home/MotorStep 的 cmd 归属校验均已确认无死循环
  - 待用户：重烧 U1（protocol_u7.c 改动在 U1 侧）+ 抓**坏时**证据：①0x0013 返回帧值 ②COM4 日志（HOME done / STEP ok / STEP reject: MOVE=0 / MOVE reject: BUSY 或 START_IT FAIL / 无 STEP 日志）→ 区分 U1 未发 / U7 拒绝 / U7 假 OK
- [2026-08-21 16:2x] ★实验结论 + U1 诊断加装：
  - 实验 1：坏时 COM4 **PING 持续有响应** → U1 的 USART1 RX **没断**；实验 2/3：0x0012 插 GND 电机停但 **8.001s 回 0x0C**（U7_Home 超时）
  - 二次铁证：坏时 COM4 抓到 `A5 5B 29 00 11 00 01 00 DC 63`（HOME 响应完整、CRC 正确、回 0x00）→ **U7 回了、U1 没收/没解析** → 且 0x0C 在精确 8.000s（=纯超时，非 DONE_ERR）→ **U1 在 8s 内完全没收到 0x00（状态机一直 S_HDR0）**，但 PING（0x21 短等待）能收 → 差异 = 长等待(8s) vs 短等待(50ms)
  - U1 当前 **LR_IROM1=0x8084 超限 116B**（加 max_retry=1 后顶爆，编译失败）→ 已【压缩 TEST 标题 12 处】省 ~180B（`[TEST] U4 Zero Sensor(0x10)...` → `[TEST] 0x10` 等）→ LR 应回 0x7FD0 限内
  - 已加 **U1_DEBUG 条件编译诊断**（protocol_u7.h 宏默认 0；protocol_u7.c：TX/TO/RX+OK/MIS 会话打印走 USB CDC）——**开 U1_DEBUG=1 时约 +140B，需同时开 Keil 优化 -O1 或再腾空间**
  - 诊断日志判读：坏时仅 `TX29+TO29` = U1 没收 0x00（物理/DMA）；`RX29 OK` 但失败 = 会话状态机；`RX21*` = PING 干扰
  - 待用户：U1_DEBUG=0 重编译烧录验证编译通过+max_retry=1 生效；仍坏则 U1_DEBUG=1+开优化抓 COM10 日志
- [2026-08-21 16:4x] ★★★★重启后首命令失败【最终根因】+ 修复：
  - U1_DEBUG 抓包：坏时 COM10 **只有 "54"(T=TX 打印) 全程无 "52"(R=RX 打印)** → U1 8s 内【完全没收帧】；COM4 证实 U7 回了 0x00（37.710）；0x05 = 35.713+8.000s（U7_Home 超时）
  - **用户新规律：成功的时段发多少遍都不坏；【重启后发的第一次必坏】** → 根因：**U7 上电先跑 TMC 配置（U7_DEBUG=1 下 BSP_TMC_Test/SetDefaults 打印大量 [DBG] 文本走 USART1=U1 RX 链路）→ 几百字节灌进 U1 的 USART1 RX DMA 环形缓冲(256B) 多次回绕 → FIFO/sync 错位 → 重启后立即发的 U7 命令响应(0x00)丢失 → 8s 超时**；TMC 打印结束后 DMA 干净 → PING/后续命令正常 → "重启后首命令必坏、之后都好"
  - ★修复（U1 main.c 一行）：`BSP_UART_Init()` 后加 `HAL_Delay(2000)` 等 U7 完成上电 TMC 配置（打印结束、DMA 干净）再进主循环。U7_DEBUG=0（生产）无打印本无污染，但延迟对两者都安全（U4 报文 2s 内溢出丢帧无害）
  - 待用户：U1_DEBUG=0 重编译烧录 → **关键验证：断电重启后【立即】发 0x0013 → 应成功**；连续多次 → 全过；再次重启重复验证
- [2026-08-21 16:5x] ★★延迟 2s 修复【无效】（用户上电后 3 次定点全失败 0x05，8s 超时；COM4 证实 U7 每次回 0x00；U1 横幅延迟确认 HAL_Delay 生效）→ **根因升级：U7 上电 TMC [DBG] 打印（几百字节走 USART1）在 U1 的 HAL_Delay 期间灌烂 DMA 环形缓冲多次回绕，即使 ClearBuffer 也无法彻底恢复** → **根治：把 U7 的 TMC 打印【默认全关】**：
  - `bsp_tmc2209.h` 新增 `U7_TMC_PRINT` 宏（默认 0，用户 #if #endif 风格）；`bsp_tmc2209.c` 的 tmc_print/tmc_print_hex/tmc_print_reg 全包 #if U7_TMC_PRINT（=0 时空实现，零输出零字符串占用）；`main.c` 的 TMC CFG OK/FAIL 打印也改包 U7_TMC_PRINT
  - 关闭无功能影响（打印仅调试）；U7_DEBUG=1 保留（HOME/STEP 命令诊断照旧，命令时短打印 + ClearBuffer 无污染）；U1 HAL_Delay(2000) 保留（等 U7 TMC 配置完成）
  - 待用户：重编译 U7（U7_TMC_PRINT=0）+ U1 → 重启后立即发 0x0013 → 应成功
- [2026-08-21 17:2x] ★验证 + 残留偶发失败修复：
  - U7_TMC_PRINT=0 生效后【失败概率大幅降低】（主要根因消除），残留：极少数上电发命令电机不转（反复重启途中偶发），重启可恢复、成功占多
  - 残留根因：**U7 上电 TMC 配置时间不稳定**——SetDefaults 读回失败重试 3 次 × 5 寄存器（每次 ~200ms）+ IsAlive 3 轮 → 最坏 3-5s 才进主循环 → 0x29 在 U7 DMA 等待 → U7 配置完才回零（6.4s）→ 总时长可 >U1 8s 超时 → 偶发失败
  - ★修复：U1 protocol_u7.c `U7_Home` 超时 **8000 → 15000ms**（覆盖 U7 最坏配置 5s + 回零 6.4s + 余量；正常情况 U7 配置 ~1s 无感）
  - 待用户：重编译 U1 → 反复断电重启 + 立即发 0x0013/0x0012 → 成功率应接近 100%（仅剩 TMC 硬件彻底故障时超时，属应暴露的硬件问题）
- [2026-08-21 17:3x] ★用户洞察修正 + 两项最终调整：
  - **用户判断正确：失败与超时长短无关**——成功时 U1 立即收到 0x00（U7 配置慢不是问题），失败时等满超时也收不到（帧丢失）→ 15s 只是让失败空等更久
  - 最终调整：①U1 `U7_Home` 超时 **15000 → 8000ms**（回零最坏 6.4s + 余量；失败本质是帧丢失，等再久也没用）；②U7 `main.c` 的 U7_DEBUG 上电打印段（CR1/SR 寄存器值 + ADC 验证 ~40B，在 TMC_PRINT 控制之外，U7_DEBUG=1 时上电仍输出 → 仍可能污染 U1 DMA）**纳入 `U7_TMC_PRINT`（默认 0）**——U7 上电现在【完全零输出】（BSP_TMC_Test 调用保留但内部 tmc_print 已空化）
  - 待用户：重编译 U7 + U1 → 反复断电重启 + 立即发命令 → 若失败归零 = 确认"任何 U7 上电打印污染"根因；若仍极偶发 = 硬件/电气层面（线缆、TMC 供电抖动）
- [2026-08-22 14:4x] ★定点移动方向 bug 修复（用户发现，判断正确）：回零方向 `MOTOR_HOME_DIR=CW`（顶到 IN1 限位端点），但 MotorSeq 的 MOVE 误用 `U7_MOTOR_CW` → 与回零同向 = 继续顶限位 → 电机失步物理不动 → 表现为"只走两段"。已改 `app.c:749` MOVE 方向 CW→**CCW**（反向进入行程区，远离限位），注释 + 命令大全同步（0x0012 CW/3200步/6.4s、0x0013 CCW 说明）。定点移动三段推进逻辑本身正确（seq_step 0→1→2）。
- 待用户：重编译 U1（改动仅在 U1 app.c）→ 0x0013 应回零后 CCW 走满三段 206/103/103 步，卡尺核对 1/1.5/2mm。
- [2026-08-22 15:0x] ★功能验收完成 + 交付：0x0013 方向修正后**功能完美实现**（A/B 组各 5 用例全 PASS，卡尺 1/1.5/2mm 达标，多次重启 100% 成功）。已产出《信息展示/电机回零与定点移动功能总结.md》（完整调用路径/关键参数/调试历程 7 项根因/验收/注意事项）。命令大全 3 处过时描述已修正：0x0012 功能行"反转"→"正转"、0x29 段（反转→CW、≤3.2s→≤6.4s、1600→3200 步）；U7 protocol.h:18 注释同步（CW/低电平触发/≤6.4s）。
